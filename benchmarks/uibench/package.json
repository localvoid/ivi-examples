{
  "private": true,
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "rollup -c ./rollup.config.mjs",
    "dev": "rollup -c ./rollup.config.mjs --watch",
    "serve": "serve",
    "gh-publish": "mkdir -p ../../docs/benchmarks/uibench && cp index.html ../../docs/benchmarks/uibench/ && cp -r dist ../../docs/benchmarks/uibench/"
  },
  "dependencies": {
    "ivi": "^3.0.0-beta.1",
    "@ivi/tpl": "^1.0.0-beta.1",
    "@ivi/template-compiler": "^3.0.0-beta.1"
  },
  "devDependencies": {
    "@ivi/rollup-plugin": "^3.0.0-beta.1",
    "@rollup/plugin-node-resolve": "^15.0.1",
    "@rollup/plugin-terser": "^0.4.0",
    "@rollup/plugin-typescript": "^11.0.0",
    "typescript": "^5.0.2",
    "rollup": "^3.20.0",
    "tslib": "^2.5.0"
  }
}
