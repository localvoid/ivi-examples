{
  "private": true,
  "name": "uibench_fc",
  "version": "1.0.0",
  "scripts": {
    "serve": "../../../node_modules/.bin/webpack-dev-server --open --config webpack.dev.js",
    "dist": "../../../node_modules/.bin/webpack --config webpack.prod.js",
    "dist:rollup": "../../../node_modules/.bin/tsc && ../../../node_modules/.bin/rollup -c rollup.config.js",
    "gh-publish": "mkdir -p ../../../docs/benchmarks/uibench && cp dist/* ../../../docs/benchmarks/uibench/"
  }
}